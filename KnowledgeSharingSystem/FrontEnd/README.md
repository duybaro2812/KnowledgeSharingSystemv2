# FrontEnd

## Run

1. `npm install`
2. `npm run dev`

Default API base: `http://localhost:3000/api`.

If needed, copy `.env.example` to `.env` and adjust:

`VITE_API_BASE_URL=...`

## Build check

- `npm run build`
